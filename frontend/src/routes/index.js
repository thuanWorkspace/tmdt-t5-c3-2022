
import HomePage from "../pages/home/HomePage";

const routes = {
    publicRoutes: [
        {
            id: 1,
            path: '/',
            page: <HomePage />
        }
    ]
}

export default routes;